services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - EstatePermit-data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/EstatePermit.db

volumes:
  EstatePermit-data:
